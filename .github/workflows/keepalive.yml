name: Keepalive
on:
  schedule:
    - cron: '*/8 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://wechatbot-g6ez.onrender.com/api/health
      - run: curl -s https://pdf-converter-idfi.onrender.com/
