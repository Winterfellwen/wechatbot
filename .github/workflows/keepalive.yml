name: Keepalive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sSfo /dev/null --max-time 60 https://wechatbot-api-vfje.onrender.com/api/health
        timeout-minutes: 2
